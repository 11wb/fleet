defaultNamespace: apisix
helm:
  chart: apisix/apisix
  repo: https://charts.apiseven.com
  version: 3.13.0 # Replace with latest if needed
  releaseName: apisix
  values:
    gateway:
      type: LoadBalancer
    etcd:
      enabled: true

