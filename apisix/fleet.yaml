defaultNamespace: apisix
helm:
  chart: apisix/apisix
  repo: https://charts.apiseven.com
  version: 2.11.2
  releaseName: apisix
  values:
    gateway:
      type: LoadBalancer
    etcd:
      enabled: true

